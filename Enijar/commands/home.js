var commando = require('../../commando/commando').commando;

/**
 * Usage: /sethome {name}
 */
commando('sethome', function (args, player) {
    echo(player, 'TODO: set home for player: ', player.name);
});
